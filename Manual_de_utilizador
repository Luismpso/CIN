# 📖 Manual de Utilizador
## Planeamento de Rotas Multimodais (Grande Porto)

Este manual descreve os procedimentos necessários para configurar o ambiente, preparar os dados e executar o sistema de otimização de rotas multimodais desenvolvido no âmbito da Unidade Curricular.

---

## 🛠️ 1. Requisitos do Sistema

Para executar este projeto, o seu sistema deve cumprir os seguintes requisitos:

* **Sistema Operativo:** Windows, macOS ou Linux.
* **Linguagem:** Python 3.8 ou superior.
* **Interface:** Jupyter Notebook ou Jupyter Lab.
* **Memória RAM:** Recomendado no mínimo **8GB** (devido ao processamento de grafos complexos do OSM e GTFS).
* **Internet:** Necessária na primeira execução para descarregar mapas via API do OpenStreetMap.

---

## ⚙️ 2. Instalação e Configuração

### Passo 1: Clonar o Repositório
Abra o terminal/linha de comandos e clone o repositório para a sua máquina local:

```bash
git clone [https://github.com/Luismpso/CIN.git](https://github.com/Luismpso/CIN.git)
cd NOME-DO-REPO

### Passo 2: Instalar dependências

O projeto utiliza várias bibliotecas para processamento geoespacial e algoritmos evolutivos. Recomenda-se a utilização de um ambiente virtual (venv ou conda).

Crie um ficheiro requirements.txt na raiz do projeto (caso não exista) com o seguinte conteúdo:
numpy
pandas
networkx
osmnx
matplotlib
deap
geopy
jupyter
scikit-learn

Instale as bibliotecas executando:
pip install -r requirements.txt

## 📂 3. Preparação dos Dados

Para que o grafo multimodal seja construído corretamente, a estrutura de ficheiros deve ser rigorosamente respeitada. O projeto baseia-se em dados GTFS (General Transit Feed Specification) e dados geográficos.

Certifique-se de que a estrutura de pastas na raiz do projeto é a seguinte:

/ (raiz)
├── bus/                <-- Colocar aqui os .txt do GTFS da STCP (stops.txt, routes.txt, etc.)
├── transit/            <-- Colocar aqui os .txt do GTFS do Metro do Porto
├── walk/               <-- Pasta para cache do grafo pedonal (criada automaticamente)
├── functions.py        <-- Ficheiro com a lógica dos algoritmos
├── main.ipynb          <-- Notebook principal de execução
└── ...

Nota: Se as pastas bus ou transit estiverem vazias, o algoritmo não conseguirá mapear os transportes públicos e falhará.

🚀 4. Execução do Programa

